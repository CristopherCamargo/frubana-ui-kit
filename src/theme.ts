import { DefaultTheme } from 'styled-components';

const defaultTheme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    default: 'gray',
    primary: '#8fb812',
    secondary: 'magenta',
  },
};

export { defaultTheme };
